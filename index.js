/**
 * @file
 * Mobile App entrypoint
 *
 * We keep a single `index.js` on the root to preserve a generic RN project
 * configuration. (Webpack is configured to use another file as entry for the web app)
 */

import './modules/mobile';
