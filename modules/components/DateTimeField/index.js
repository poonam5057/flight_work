/**
 * @file
 * Datetime input component used on Mobile
 *
 * @format
 * @flow strict-local
 */

// For the time being we use the same picker as on web
import DateTimeField from './index.web';

export default DateTimeField;
